# 🔥 家族アプリ 同期設定ガイド

家族みんなのスマホでデータを共有するには、Googleの無料データベース「Firebase」の設定が必要です。
パソコンで以下の手順を行ってください（5分くらいで終わります）。

## 1. Firebaseプロジェクトを作る
1. [Firebase Console](https://console.firebase.google.com/) にアクセス。
2. 「プロジェクトを作成」をクリック。
3. 名前は適当に（例: `my-family-app`）入れて、続行。
4. 「Googleアナリティクス」はオフでOK。「プロジェクトを作成」を押す。

## 2. データベースを作る
1. プロジェクトができたら、左メニューの **「Build」** または **「構築」** から **「Firestore Database」** を選ぶ。
2. **「データベースの作成」** をクリック。
3. 場所は `nam5 (us-central)` のままでOK（無料枠が大きい）。
4. **【重要】** セキュリティルール設定で **「本番環境モードで開始する」** を選んで次へ。

## 3. アプリと繋ぐ
1. 左上の「プロジェクトの概要（家のアイコン）」の横にある歯車⚙️を押して **「プロジェクトの設定」** を開く。
2. 下の方にある「マイアプリ」の `</>` (Web) アイコンをクリック。
3. アプリのニックネームを入れて（例: `family-app`）、「アプリを登録」。
4. 画面にコードが出てきます。`const firebaseConfig = { ... };` の `{` から `}` まで（カッコ含む）をコピーしてください。

これが「合鍵」です！

## 4. アプリに入力
1. 家族アプリの設定画面（⚙️）を開く。
2. PINコード（初期は `0000`）を入力して管理者エリアを開く。
3. 「☁️ クラウド同期設定」のボックスに、さっきコピーしたコードを貼り付ける。
4. 「設定を保存」ボタンを押す。

## 5. セキュリティルールの設定（最後！）
今のままだと誰も書き込めません。「誰でも読み書きOK（ただしURLを知ってる人だけ）」に変更します。
（※本来は認証が必要ですが、家族用なので簡易設定です）

1. Firebase Consoleに戻り、**「Firestore Database」** > **「ルール」** タブを開く。
2. コードを以下のように書き換えて「公開」を押す。

```
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
```

これで完了です！🎉
家族全員のスマホで同じ設定を行ってください（APIキーは同じものを使います）。
